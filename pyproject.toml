[project]
name = "lakehouse-chat"
version = "0.1.0"
description = "Natural language chat interface to the KBase/BERDL Data Lakehouse"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "nicegui>=3.6.1",
    "python-dotenv>=1.2.1",
]
